import { Formik, Form, Field, ErrorMessage } from "formik"
import * as yup from "yup"

export function Register() {
    return(
        <div className="border h-40 w-64">
            world
        </div>
    )
}